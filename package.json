{
  "name": "pdf-bookmark-embedder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Simple PDF bookmark embedding tool for iOS Safari",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "python server/bookmark_server.py",
    "network": "./start-network.sh",
    "start": "./start-network.sh",
    "prod": "./start_production.sh",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
