{
    "version": 2,
    "builds": [
        {
            "src": "server/bookmark_server_clean.py",
            "use": "@vercel/python"
        },
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server/bookmark_server_clean.py"
        },
        {
            "src": "/(.*)",
            "dest": "/dist/$1"
        }
    ]
}